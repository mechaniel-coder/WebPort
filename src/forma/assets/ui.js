/**
 * Forma interaction boot.
 *
 * Widget implementations are shared across all three sites — see
 * /shared/widgets.js.
 */
import { initAll } from '../../shared/widgets.js';

initAll({
  forms: {
    successMessage:
      'Thanks — we would normally reply within a day. This demonstration store does not ' +
      'transmit anything.',
  },
});
