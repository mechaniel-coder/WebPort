/**
 * Northwind interaction boot.
 *
 * Widget implementations are shared across all three sites — see
 * /shared/widgets.js.
 */
import { initAll } from '../../shared/widgets.js';

initAll({
  forms: {
    successMessage:
      'Thanks — a solutions engineer would normally be in touch within one business day. ' +
      'This demonstration site does not transmit anything.',
  },
});
